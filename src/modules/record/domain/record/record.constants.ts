export enum RunningState {
  RUNNING = 'RUNNING',
  STOP = 'STOP',
  FINISH = 'FINISH',
}
export enum RecordTypeEnum {
  free = 'free',
  distance = 'distance',
  time = 'time',
}

// 사용할 지 고려중
export enum GoalTypeEnum {
  DISTANCE = 'DISTANCE',
  TIME = 'TIME',
}
